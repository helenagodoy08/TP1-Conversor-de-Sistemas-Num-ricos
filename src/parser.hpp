#ifndef parser_HPP
#define parser_HPP

#include <string>

using namespace std;

//utiliza-se '&' para retornar a main os valores alterados aq no parser
void separador(string valor, string &inteiro, string &frac);

#endif